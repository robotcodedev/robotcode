# Tasks: library-keyword-set-declaration

## 1. Prerequisites

- [ ] 1.1 Confirm that `library-loading-robustness` is applied: its hard timeout and remembered failures must be in place, because every argument set of an `args` library is loaded separately; its task 5.1 must provide the module-level dict of the run-time-only built-ins, the resolution function next to `resolve_args` and the `ImportsManager` method of its D5, which D2 and D4 use; and its `test_library_loading.py` provides the real-`ImportsManager` test setup reused in 3.1; verify `hatch run test:test -- tests/robotcode/robot/diagnostics` is green, including the robustness change's timeout tests
- [ ] 1.2 Record the maintainer's answers to Q1, Q4, Q5 and Q6 in `design.md`, use the attribute name decided for Q1 everywhere below and replace "the keyword-set attribute" in `specs/library-keyword-set-declaration/spec.md` with it; verify `openspec validate library-keyword-set-declaration` passes

## 2. Declaration and run-time arguments

- [ ] 2.1 In `packages/robot/src/robotcode/robot/diagnostics/library_doc.py` add the enum `KeywordSet` (`ARGS`, `VOLATILE`) next to `LibraryType`, add `LibraryDoc.keyword_set: Optional[KeywordSet] = None` and `LibraryDoc.runtime_only_args: Optional[List[str]] = None` (neither part of `__eq__`/`__hash__`), and in `get_library_doc` read the attribute with `getattr` on the object `_import_test_library` returned, before `_get_test_library`, normalised like RF normalises `ROBOT_LIBRARY_SCOPE` (D1); verify with a new `tests/robotcode/robot/diagnostics/test_library_keyword_set.py` that calls `get_library_doc` in-process on library files written to `tmp_path`, each with a unique module name: a class with `args`, a module with `volatile`, a by-path subclass of a declaring class, an attribute set only on the instance in `__init__` (`None`), `"ARGS"` and `"Args"`, `"arguments"` (`None`, no error) and a class whose `__init__` raises for the given arguments (declaration still set); extend `tests/robotcode/robot/diagnostics/test_library_doc_pickle.py` so both fields survive a pickle round trip and a state without them yields `None`
- [ ] 2.2 Add one helper to `library_doc.py` that returns the resolved arguments still containing a variable (`contains_variable(arg, "$@&%")`); in `get_library_doc`, when the declaration is `ARGS` or `VOLATILE`, resolve the arguments with the resolution function of `library-loading-robustness` D5 and, if the helper finds any, build the library with `()`, record no error for the unresolved variables and set `runtime_only_args` (D4); verify in `test_library_keyword_set.py` on the RF matrix (`hatch run test:test -- tests/robotcode/robot/diagnostics/test_library_keyword_set.py`) that a library declared `args` and one declared `volatile`, imported with `${UNDEFINED}`, return the keywords of the load without arguments, `runtime_only_args == ["${UNDEFINED}"]`, no `Variable '${UNDEFINED}' not found.` error and no `loaded_without_arguments` marker of `library-loading-robustness`, that `%{NOPE_XYZ}` counts as run-time only and an escaped `\${X}` does not, and that an undeclared library still reports `Variable '${UNDEFINED}' not found.` with the keywords of the load without arguments
- [ ] 2.3 Verify the run-time-only built-ins in the worker (D4). No change to `resolve_args`, `resolve_robot_variables` or `_get_default_variables` here: the resolution without them comes from `library-loading-robustness` task 5.1. Verify in `test_library_keyword_set.py` on the RF matrix (`hatch run test:test -- tests/robotcode/robot/diagnostics/test_library_keyword_set.py`):
  - the resolution function of `library-loading-robustness` D5, which the worker calls, leaves `${OUTPUT DIR}/x`, `${output_dir}/x`, `${SUITE NAME}`, `@{TEST TAGS}`, `&{SUITE METADATA}` and a Variables-section variable `${X}` with the value `${OUTPUT DIR}/foo` unresolved, and resolves `${TEMPDIR}`, `${CURDIR}/x`, `${EMPTY}` and a command-line variable named `OUTPUT DIR`;
  - a declared library that records its initializer arguments to a file in `tmp_path` is loaded without arguments for `${OUTPUT DIR}/x` and for `${SUITE NAME}`, with `runtime_only_args == ["${OUTPUT DIR}/x"]` and `["${SUITE NAME}"]`, and never receives the placeholder value;
  - the same library receives `${TEMPDIR}` and `${CURDIR}/x` with their values;
  - an undeclared copy imported with `${OUTPUT DIR}/x` still receives `/x`, reports no error and has no `runtime_only_args` (Q6's recommended default);
  - the existing tests under `tests/robotcode/robot/diagnostics` pass unchanged.
- [ ] 2.4 Only if Q6 is answered differently from the recommended default: apply the run-time rule of D4 to undeclared libraries as decided, add it to the spec delta (the requirement "Import arguments whose values are only known at run time" and its scenario for undeclared libraries), and verify in `test_library_keyword_set.py` that an undeclared library imported with `${OUTPUT DIR}/x` behaves as decided — otherwise mark this task as not applicable

## 3. Disk cache and namespace cache

- [ ] 3.1 In `packages/robot/src/robotcode/robot/diagnostics/imports_manager.py` add `LibraryMetaData.keyword_set` with `compare=False`, get the statically resolved arguments in `get_libdoc_for_library_import_with_meta` from the `ImportsManager` method of `library-loading-robustness` D5, add them to `_LibrariesEntryKey` as a third field next to the `resolve_args` tuple and pass them through `_LibrariesEntry` to `_get_library_libdoc` (D2), build the argument-set key `<cache_key>\nargs:<sha256 of repr(resolved_args)>`, and implement the order of D3: the plain entry is read inside the existing `try`, a timed-out load keeps the handling of `library-loading-robustness`, the declaration is copied into the meta before saving, the default entry is written only while no valid plain entry exists, and a result without a declaration never overwrites a valid default entry; verify with a new `tests/robotcode/robot/diagnostics/test_imports_manager_keyword_set.py` that uses a real `ImportsManager` on `tmp_path` set up like `test_library_loading.py` (mocked document cache helper, library files backdated with `os.utime` so their state is trusted, `monkeypatch.chdir(tmp_path)` because `get_library_doc` changes the working directory, paths only through `tmp_path`) and patches `_run_in_subprocess` on the instance to run in-process and count loads: `ArgLib a` and `ArgLib b` of a declared `ArgLib(mode)` return `Kw A One` / `Kw B One`; a second `ImportsManager` on the same cache returns both without a load; `ArgLib ${MODE}` with the command-line variable `MODE=b` hits the `b` entry; an undeclared copy of the library returns the first stored set for both (first wins); a stored result with errors is not served; a load that returns an error document without a declaration leaves the default entry intact; after `os.utime` gives the library file another backdated time, the next set that loads without errors (`ArgLib c`) replaces the plain row; a meta pickled without `keyword_set` counts as a miss; `data_cache.list_entries(CacheSection.LIBRARY)` shows the plain key plus one `\nargs:` row per set; and, for the setup the author page describes (D8), a by-path `class XYService(Remote)` declaring `args` imported with the URIs of two local XML-RPC test servers (threads on `127.0.0.1`, port 0) that return different keywords is served each server's keywords by a fresh `ImportsManager` on the same cache, while plain `Remote` imported with the same two URIs is served the keywords of the first (undeclared, first wins)
- [ ] 3.2 In `_get_library_libdoc` store nothing and return no meta for `VOLATILE` results and for results with `runtime_only_args`, and when a valid plain entry says `ARGS` and the resolved arguments contain run-time-only values, return its documentation with `runtime_only_args` set and no meta, without a load (D3, D4); verify in `test_imports_manager_keyword_set.py` that a volatile library is loaded by each of two `ImportsManager` instances and leaves no `LIBRARY` row, that `ArgLib ${SUT_MODE}` with a default entry from `a` returns `Kw A One` with the marker, without a load and with meta `None`, that `ArgLib ${OUTPUT DIR}/x` does the same with the marker `["${OUTPUT DIR}/x"]`, that with an empty cache it loads without arguments, sets the marker and stores nothing, and that, with a default entry from `a`, `ArgLib /x` and `ArgLib ${OUTPUT DIR}/x` imported in either order in one `ImportsManager` get separate in-memory entries and return the keywords of `/x` without the marker and `Kw A One` with the marker `["${OUTPUT DIR}/x"]`, respectively
- [ ] 3.3 Verify the precedence of the settings (D3 steps 1–2) in `test_imports_manager_keyword_set.py`: a declared `ArgLib` in `ignored-libraries` leaves no `LIBRARY` row and loads once per argument set; with `ignore-arguments-for-library`, `ArgLib a` and `ArgLib b` both return the keywords `ArgLib` has without arguments, from one plain row
- [ ] 3.4 Confirm that `_import_library` in `packages/robot/src/robotcode/robot/diagnostics/import_resolver.py` keeps an existing `None` for `lib:{name}` (added by `library-loading-robustness` task 2.3; no change here, D6); verify with a test in `tests/robotcode/robot/diagnostics/test_namespace_cache.py` that a suite importing a declared `ArgLib    ${SUT_MODE}    AS    A` (fallback, meta `None`) followed by `ArgLib    b    AS    B` (meta) gets `None` from `build_namespace_meta`
- [ ] 3.5 Add `ImportsManager.get_libdoc_for_library_name(name, base_dir, variables=None)` (D5) and use it in `packages/language_server/src/robotcode/language_server/robotframework/parts/completion.py` (library-name completion documentation) and `keywords_treeview.py` (`getLibraryDocumentation`, `getKeywordDocumentation`); verify in `test_imports_manager_keyword_set.py` that with a default entry from `b` the lookup returns `Kw B One` without a load, that an undeclared library gives the same result as `get_libdoc_for_library_import(name, ())`, that without a default entry the library is loaded without arguments, and that `get_libdoc_for_library_import("ArgLib", ())` still returns `Kw A One` when the default entry comes from `b`; verify with a test of `_get_library_documentation` in `keywords_treeview.py`, with the imports manager mocked where that module gets it, that it returns the keywords of the name-only lookup

## 4. Hover and canonical documentation

- [ ] 4.1 In `packages/language_server/src/robotcode/language_server/robotframework/parts/hover.py` add the module-level note function of D7 and prepend its result to the library import hover when `runtime_only_args` is set; verify with unit tests of the function for one, two and three arguments and for a document without the marker, with a test using the `protocol` fixture and `open_temp_document` on a `tmp_path` suite that imports a declared library by a relative path with `${UNDEFINED}` (the import hover starts with the note naming `${UNDEFINED}`), and that the hover of an ordinary library import is unchanged (`hatch run test:test -- tests/robotcode/language_server/robotframework/parts/test_hover.py` shows no baseline diff)
- [ ] 4.2 Only if Q4 is answered differently from the recommended default: implement the decided reporting for keyword calls into an import whose documentation carries `runtime_only_args` in both analyzers, add it to the spec delta, and verify with an `analyzer_factory` test and a `_PARITY_CASES` entry in `test_variable_pipeline_comparison.py` — otherwise mark this task as not applicable
- [ ] 4.3 Only if `doc-cli` has been applied before this change (otherwise its task 4.5 does this): let the canonical renderer (`packages/robot/src/robotcode/robot/diagnostics/documentation_markdown.py`) show the note for `runtime_only_args` described in `doc-cli` design D6 (the wording of the hover note of D7), after the load errors at the start of `## Importing`, and add a MODIFIED requirement "Load errors are part of the document" of `library-documentation-markdown` with a scenario for it to this change's specs; verify with a renderer test in `tests/robotcode/robot/diagnostics/test_documentation_markdown.py` on a `LibraryDoc` with `runtime_only_args == ["${SUT_MODE}"]` (note present, `Importing` present) and one without (no note), and `openspec validate library-keyword-set-declaration` — otherwise mark this task as not applicable

## 5. Documentation

- [ ] 5.1 Write `docs/03_reference/library-keyword-sets.md` for library authors (the attribute and its values, inheritance by wrapper subclasses, what is cached per argument set, the default entry, the run-time fallback including the run-time-only built-ins such as `${OUTPUT DIR}` and its hover note, `volatile`, and the precedence of `ignored-libraries` / `ignore-arguments-for-library`), with a paragraph on `Remote` (D8: part of Robot Framework, so it cannot carry the attribute and RobotCode has no built-in rule for it; for `Remote`-based setups a subclass such as `class XYService(Remote)` that sets the attribute to `args`, or `ignored-libraries`), add a line for it to `docs/03_reference/index.md`, and add a paragraph on argument-dependent libraries to "Managing the analysis cache" in `docs/03_reference/analyzing-code.md`; verify with `npm run docs:build`
- [ ] 5.2 Confirm that `analyze-config-in-robot` is applied, then in `packages/robot/src/robotcode/robot/config/analyze_config.py` (`CacheConfig`, which it moved there with the analysis part of the configuration model) update the `ignored-libraries` description (argument-dependent libraries declare `args`; the setting stays for undeclared libraries and external state) and the `ignore-arguments-for-library` description (the setting overrides a declaration: loaded without arguments, cached under one entry); mirror the `ignored-libraries` text in the `markdownDescription` of `robotcode.analysis.cache.ignoredLibraries` in the root `package.json`; regenerate with `hatch run robotcode config info desc > docs/03_reference/config.md` and `hatch run create-json-schema`; verify that the diffs of `docs/03_reference/config.md` and `docs/public/schemas/robot.toml.json` contain only the changed descriptions

## 6. Verification

- [ ] 6.1 Run `hatch run lint:all` and `hatch run test:test` (full RF matrix 5.0–7.5) and confirm both pass with no regression-baseline changes; confirm the CI matrix passes on Linux, Windows and macOS (all new tests use `tmp_path`, `Path` joins, no hardcoded separators, relative library paths in Robot files)
- [ ] 6.2 Manually check a temporary project with a declared `ArgLib`, two suites importing `ArgLib    a` and `ArgLib    b` a third importing `ArgLib    ${SUT_MODE}` and a fourth importing `ArgLib    ${OUTPUT DIR}/x`: `robotcode analyze code` reports no `KeywordNotFound` on a fresh cache and on the second run, `robotcode analyze cache list` shows `ArgLib · args:…` rows, VS Code shows the hover note on the `${SUT_MODE}` and `${OUTPUT DIR}/x` imports, and an undeclared copy of the library still behaves as before (first stored set wins)
