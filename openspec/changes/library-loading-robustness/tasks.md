# Tasks: library-loading-robustness

## 1. Hard timeout

- [ ] 1.1 In `packages/robot/src/robotcode/robot/diagnostics/imports_manager.py` rewrite `_run_in_subprocess` with a spawned `multiprocessing` `Process`, a module-level worker target sending `(ok, value)` over a one-way `Pipe`, `poll(self.load_library_timeout)`, a dedicated timeout exception (subclass of `RuntimeError`, today's message) and a `finally` that ends a live worker (`terminate()`, `join(1)`, `kill()`, `join()`); re-raise worker exceptions with their type (a `RuntimeError` with the text if they cannot be pickled) and raise a `RuntimeError` naming the exit code when the worker ends without a result (the parent closes its copy of the sending end after `start()`); verify with a new `tests/robotcode/robot/diagnostics/test_library_loading.py` (real `ImportsManager` with `DocumentsManager`, mocked `DocumentsCacheHelper`, `tmp_path` as root and cache base): with `load_library_timeout=1`, loading a library written to `tmp_path` that sleeps 5 s at import and then writes a marker file (path embedded with `repr()`) raises the timeout exception with today's message well before 5 s, and the marker does not exist 6 s after the start; with the default timeout, `Collections` loads with its keywords, and a library whose import calls `os._exit(3)` yields an error naming exit code 3 well before the timeout; `antigravity` still raises `IgnoreEasterEggLibraryWarning` from the worker, as before
- [ ] 1.2 Confirm that variable-file loads go through the same helper; verify in `test_library_loading.py` that loading a variable file sleeping 5 s at import raises the timeout exception within the same bound

## 2. Timed-out results are kept

- [ ] 2.1 In `_get_library_libdoc` and `_get_variables_libdoc` catch the timeout exception and return an error `LibraryDoc`/`VariablesDoc` (import name, `source` from `LibraryMetaData.origin` or the found path of an import by path, `python_path`, one `Error` without a source, with today's message and type name `RuntimeError`) together with meta `None`, without writing it to the disk cache; update the tests of 1.1 and 1.2 that now get this result instead of the exception; verify in `test_library_loading.py` with a hanging library that writes one line per load to a log in `tmp_path`: two `get_libdoc_for_library_import_with_meta` calls with the same arguments and a live sentinel load once and both return the error with meta `None`; `data_cache.read_entry` has no entry for the library; a `did_change_watched_files` event for the library file makes the next call load again; a second `ImportsManager` with the default timeout on the same cache directory loads again and returns keywords once the library no longer sleeps (switched by a file in `tmp_path`); the same three checks for a variable file
- [ ] 2.2 In `packages/robot/src/robotcode/robot/diagnostics/import_resolver.py` let `_import_default_libraries` also report the `errors` of a returned default-library document with its "Can't import default library '…': …" message; verify with an `ImportResolver` test in the style of `tests/robotcode/robot/diagnostics/test_import_resolver_dependency_metas.py` whose mocked manager returns an error document for `BuiltIn`
- [ ] 2.3 In `ImportResolver._import_library` and `_import_variables` keep an existing `None` dependency meta for the key (`lib:{imp.name}`, `var:{imp.name}`) instead of replacing it; verify with `ImportResolver` tests in the style of `test_import_resolver_dependency_metas.py` whose mocked manager returns meta `None` for the first import of a name and a meta for a second import of the same name with other arguments and an alias: the recorded dependency meta stays `None` (and the other order records `None` too)
- [ ] 2.4 Verify end to end that a timed-out top-level import reports the unchanged timeout message and code `RuntimeError` on the import range through `ImportResolver` with the real `ImportsManager` of 1.1, and that its dependency meta is `None`, so `build_namespace_meta` returns `None` for the resulting namespace

## 3. Keyword names first

- [ ] 3.1 In `packages/robot/src/robotcode/robot/diagnostics/library_doc.py` change `get_library_doc_from_library` for `create_keywords=True`: create the keywords before reading `lib.doc` and building the init keyword, restore the instance with `lib.reset_instance(instance)` after `create_handlers()` on RF < 7, and after a failure record the error and build only the introduction from the library's Python docstring and the init keyword from the class `__init__` through a `KeywordWrapper` subclass whose `doc` returns the `__init__` docstring; leave `create_keywords=False` unchanged; verify with tests in `test_library_loading.py` on all RF versions using a dynamic library in `tmp_path` that records its calls to a file: a working library calls `get_keyword_names` before `get_keyword_documentation('__intro__')`/`('__init__')`, is instantiated once, and its introduction and init documentation are the dynamic ones; a library whose `get_keyword_names` raises gets no `get_keyword_documentation` call, reports "Getting keyword names from library '…' failed", and keeps its init arguments with the `__init__` docstring and the class docstring as introduction; `tests/robotcode/robot/diagnostics/test_library_doc_from_instance.py` stays green
- [ ] 3.2 Add a `Remote` test: a local TCP server on `127.0.0.1` (port 0) that accepts and closes connections and counts them; `get_library_doc("Remote", (uri,))` makes as many connections as `Remote(uri).get_keyword_names()` alone, reports the keyword-name error, and its init has the arguments `uri` and `timeout` with the `Remote.__init__` docstring; verify on all RF versions

## 4. Visible fallback without arguments

- [ ] 4.1 Add `LibraryDoc.loaded_without_arguments: bool = False` and set it in `get_library_doc` when the documented instance comes from the retry with `()`; verify in `test_library_loading.py` with `StrictLib` (raises for an unknown `mode`) written to `tmp_path`: `("bogus",)` sets the marker, keeps the initialization error and provides `Strict Keyword`; `("a", "b", "c")` and `("${NOT_KNOWN}",)` set it too; `()` does not; a library with a required argument and `("${NOT_KNOWN}",)` does not (no keywords); a pickle round-trip keeps the field and a state dict without it yields a falsy value (extend `tests/robotcode/robot/diagnostics/test_library_doc_pickle.py`)
- [ ] 4.2 Add `LIBRARY_LOADED_WITHOUT_ARGUMENTS = "LibraryLoadedWithoutArguments"` to `errors.py` and report it as INFORMATION in `ImportResolver._report_entry_errors` after the import's errors ("Keywords of library '<name>' come from loading it without arguments, because loading it with the import's arguments failed."); verify with `ImportResolver` tests: a marked document yields the error diagnostics followed by the information on the import range; an unmarked document with errors yields none; a marked document imported through a resource file yields none in the importing suite; a library matching `ignore-arguments-for-library` yields none (real `ImportsManager`)
- [ ] 4.3 Only if `doc-cli` has been applied before this change (otherwise its task 4.5 does this): let the canonical renderer (`packages/robot/src/robotcode/robot/diagnostics/documentation_markdown.py`) show the note for `loaded_without_arguments` described in `doc-cli` design D6, after the load errors at the start of `## Importing`, and add a MODIFIED requirement "Load errors are part of the document" of `library-documentation-markdown` with a scenario for it to this change's specs; verify with a renderer test in `tests/robotcode/robot/diagnostics/test_documentation_markdown.py` using `StrictLib` with `("bogus",)` (error, then the note) and `()` (no note), and `openspec validate library-loading-robustness` — otherwise mark this task as not applicable

## 5. Library imports that Robot Framework ignores

- [ ] 5.1 In `packages/robot/src/robotcode/robot/diagnostics/library_doc.py` let `_get_default_variables` take the runtime-only built-ins of design D5 from one module-level dict, and add next to `resolve_args` a function that resolves import arguments against the default variables without that dict, adding `${CURDIR}`, `${EXECDIR}`, the command-line variables and the file's variables through a private helper shared with `resolve_robot_variables` (`resolve_args` and `resolve_robot_variables` unchanged); add a small `ImportsManager` method that returns the statically resolved import arguments through it, with the inputs of the entry key of `get_libdoc_for_library_import_with_meta`, whose entry keys keep `resolve_args`; verify in `test_library_loading.py` with the real `ImportsManager` that the method returns `${OUTPUT DIR}`, `${output_dir}/a` and `${LOG}` (a scope variable with the value `${OUTPUT DIR}/x`) as written and `a/${OUTPUT DIR}` for `${VAL}/${OUTPUT DIR}` with `${VAL}` = `a`, and resolves `${TEMPDIR}`, `${EMPTY}`, `${VAL}` and a command-line variable named `OUTPUT DIR`; that a library written to `tmp_path` and imported once with `${OUTPUT DIR}` and once with `${EMPTY}` is loaded once, as today (load log); that a variable file imported with the argument `${OUTPUT DIR}/x` still receives `/x`; and that the existing tests under `tests/robotcode/robot/diagnostics` pass unchanged
- [ ] 5.2 Add `LIBRARY_IMPORT_IGNORED = "LibraryImportIgnored"` to `errors.py`; in `ImportResolver` store the resolved arguments per registered library name (default libraries with `()`) and, behind a module-level `RF_VERSION >= (7, 4)` flag, report the WARNING in `_import_library` for `top_level` imports whose name is taken and `already_imported` is `None`, per design D5 (no warning if either document has errors; "Another library with name '<name>' …" for different library names; "Library '<name>' has already been imported with different arguments. …" for different resolved arguments; no warning if a resolved argument still contains a variable), with the earlier import as related information; verify with a new `tests/robotcode/robot/diagnostics/test_import_resolver_ignored_imports.py` covering every scenario of the `library-imports` spec requirement "Library imports that Robot Framework ignores are reported" (different arguments, `a` then `x=a`, another library under a taken name, taken alias, `${VAL}` resolving to the same value, unknown variable, `${OUTPUT DIR}`, `${OUTPUT DIR}` through a file variable, `${EMPTY}` with a fixed value, earlier import in a resource file, ignored import inside a resource file, a library that fails to load), the inverse gate on RF < 7.4, and suppression through `DiagnosticsModifier` with `# robotcode: ignore[LibraryImportIgnored]`
- [ ] 5.3 Check once during implementation, on RF 7.5 and RF 7.4, with a project run through `ImportResolver` and a real `ImportsManager`, that the warnings appear on exactly the lines of the suite for which `robot` prints the warning "has already imported … This import is ignored." for the same suite, except for imports whose arguments refer to a runtime-only built-in such as `${OUTPUT DIR}`, where only `robot` warns (design D5) (a one-off comparison; the permanent tests are those of 5.2)

## 6. Open questions (only if confirmed)

- [ ] 6.1 If the recommendation for "Duplicate loads without arguments" is confirmed: in `packages/language_server/src/robotcode/language_server/robotframework/parts/signature_help.py`, `inlay_hint.py` and `packages/robot/src/robotcode/robot/diagnostics/semantic_analyzer/analyzer.py` use the inits of the import's own `LibraryDoc` for library imports and never load the library again with `()` (also when it has no inits), keeping the variable-file reloads; verify with `tests/robotcode/language_server/robotframework/parts/test_signature_help_model.py` / `test_inlay_hint_model.py` cases for a library whose arguments fail (argument names shown, no second load recorded) — otherwise mark this task as not applicable
- [ ] 6.2 If the recommendation for "Failed variable-file loads on disk" is confirmed: set `has_errors` for variable-file results with errors in `_get_variables_libdoc` and skip such entries when reading the disk cache, as for libraries; verify in `test_library_loading.py` that a variable file raising at import is loaded again by a second `ImportsManager` on the same cache directory — otherwise mark this task as not applicable

## 7. Documentation and verification

- [ ] 7.1 In `docs/03_reference/analyzing-code.md` add `LibraryImportIgnored` (Robot Framework 7.4+) and `LibraryLoadedWithoutArguments` to the import diagnostics, saying for `LibraryImportIgnored` that an import whose arguments have no static value (an unknown variable, or a built-in such as `${OUTPUT DIR}` that Robot Framework only sets during the run) gets no warning, and a short note next to the `load-library-timeout` example: a load that exceeds it is stopped, reported on the import and retried when the library changes or the language server restarts; verify with `npm run docs:build`
- [ ] 7.2 Run `hatch run lint:all` and `hatch run test:test` (full RF matrix incl. `rf74`, `rf75`) and confirm both pass with no regression-baseline changes; confirm the CI matrix passes on Linux, Windows and macOS
- [ ] 7.3 Manually check in VS Code on RF 7.5: `Library    Remote    http://10.255.255.1:8270` with `load-library-timeout = 5` reports the timeout after about 5 s and editing the suite does not start a new load; `Library    StrictLib.py    bogus` shows the error and the `LibraryLoadedWithoutArguments` information; the duplicate-import suite shows the same warnings as `robot`, apart from the runtime-only built-ins of design D5; and with `robotcode analyze code` that the Remote run finishes in about 5 s instead of minutes
