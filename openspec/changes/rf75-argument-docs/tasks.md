# Tasks: rf75-argument-docs

## 1. Prerequisites

- [ ] 1.1 Confirm `support-rf75` tasks 1.1 and 2.1–2.4 are done (`rf75` env, docstring helper with `parse_docstring`, type-doc format, return-type normalisation); verify `hatch run test.rf75:test -- tests/robotcode/robot/diagnostics` is green

## 2. Documentation model

- [ ] 2.1 In `packages/robot/src/robotcode/robot/diagnostics/library_doc.py` add `ArgumentInfo.doc: str = ""` and `ArgumentInfo.type_docs: Optional[Dict[str, str]] = None`, `KeywordDoc.return_doc: str = ""`, `KeywordDoc.raises: Optional[List[Tuple[str, str]]] = None`, `KeywordDoc.return_type_docs: Optional[Dict[str, str]] = None`, and make `KeywordDoc.__setstate__` fall back to field defaults; verify with a pickle round-trip test next to `tests/robotcode/robot/diagnostics/test_data_cache.py` that the fields survive `pickle.loads(pickle.dumps(...))` and that a state dict lacking them yields the defaults
- [ ] 2.2 Extend the `support-rf75` docstring helper to return `DocInfo.args/returns/raises`, store them at the three keyword sites (library initializer, library keywords, resource keywords) on RF ≥ 7.5, use `info.doc` as the documentation text there and delete the `support-rf75` branch that kept the original text when sections exist; update `test_library_doc_docstrings.py` so the `Args:` text is asserted present in `doc` only on RF < 7.5 and absent on RF ≥ 7.5; verify by extending `tests/robotcode/robot/diagnostics/test_library_doc_docstrings.py` with a fixture keyword `paint(shade, *items) -> …` documenting `Args:` (multi-line entry, `*items`), `Returns:` and `Raises:`, and a resource keyword with `Args:` `${name}: Who to greet.`: on RF ≥ 7.5 the fields are filled and `doc` contains no section header; on RF < 7.5 the fields are empty and `doc` is unchanged; the same via `get_library_doc_from_library(create_keywords=False)` / `get_resource_doc_from_resource` with the RF objects unchanged; an `Args:` entry for a non-existing argument produces no error
- [ ] 2.3 Copy RF's per-argument `type_docs` map (already computed in `_get_type_docs`) into `ArgumentInfo.type_docs`, walk the return type like RF's `TypeDocBuilder` into `KeywordDoc.return_type_docs`, and add `LibraryDoc.get_types_for_argument(arg)` (values of `arg.type_docs`, falling back to `get_types(arg.types)`); verify by extending the `support-rf75` type-alias test (skipped below Python 3.12/RF 7.5) with `type Shade = Color` (Enum), `type Coord = Point` (TypedDict), `type Mixed = Color | int`, `-> Shade` asserting `type_docs == {"Color": "Color"}` etc. and that `get_types_for_argument` yields the `Color` TypeDoc, plus an RF ≥ 6.1 assertion on a fixture library keyword `def kw(self, base: int)` written to `tmp_path` that `get_types_for_argument` returns the `integer` TypeDoc (BuiltIn's `Convert To Integer` is annotated only from RF 7.4 on, so it may serve as an additional RF ≥ 7.4 case only)

## 3. Rendering

- [ ] 3.1 Create `packages/robot/src/robotcode/robot/utils/markdown_docs.py` with pure functions: `shift_headings`, `render_toc` (two levels, shared slug helper), `resolve_reference_links(text, targets, link_resolver)` (skips code spans, fences, images, escaped brackets and keyword-local definitions; caseless/spaceless matching; inlines introduction reference definitions), `normalize_admonitions` (kinds matched case-insensitively, e.g. `> [!note]`); verify with `tests/robotcode/robot/utils/test_markdown_docs.py` covering each rule and each exclusion, on all RF versions without RF objects
- [ ] 3.2 Extend `KeywordDoc._get_signature` with the description list (only when any argument has a description), `**Return Type**: \`T\` — description` / `**Returns**: description`, and `**Raises**:` items; add a keyword-only `link_resolver` parameter to `KeywordDoc.to_markdown`, `LibraryDoc.to_markdown`, `VariablesDoc.to_markdown` (which overrides and must forward it) and `TypeDoc.to_markdown`, apply the Markdown normaliser (headings, `%TOC%`, admonitions, reference links, no backtick auto-linking) in their `MARKDOWN` branches on every RF version, and extend `_create_toc` to two levels; verify with unit tests that BuiltIn `Log` on RF 7.5 renders the description list, no `Args:` block and `` `Set Log Level` ``, that the BuiltIn library rendering has no literal `%TOC%`, no `# ` line and a TOC, that a Collections keyword's admonition renders as `> **Warning**`, that a fixture library with `ROBOT_LIBRARY_DOC_FORMAT = "MARKDOWN"` written to `tmp_path` is normalised identically on RF 7.4 and 7.5 (golden string), and that a Robot-format library's and a variables file's rendering are byte-identical to before (golden strings in the test)
- [ ] 3.3 Update `tests/robotcode/repl/test_dot_commands.py`'s fake `KeywordDoc.to_markdown` to accept the new keyword-only parameter and run `hatch run test:test -- tests/robotcode/repl tests/robotcode/robot`; verify green on all RF versions

## 4. Consumers

- [ ] 4.1 In `packages/language_server/.../parts/signature_help.py` (model and legacy paths) build the parameter documentation as argument description + `get_types_for_argument` type docs, and in `completion.py` use `get_types_for_argument` for value completion and add lazy documentation (via `doc_cache`) to named-argument items; verify by extending `tests/robotcode/language_server/robotframework/parts/test_signature_help_model.py` (parameter documentation starts with the description and contains the TypeDoc header) and adding a completion test (protocol fixture as in `test_hover.py`) asserting the resolved `level=` item for `Log` contains "The log level to use." on RF ≥ 7.5 and the `int` → `integer` lookup on all versions; confirm `hatch run test:test -- tests/robotcode/language_server/robotframework/parts/test_hover.py tests/robotcode/language_server/robotframework/parts/test_signature_help.py` produces no baseline diffs
- [ ] 4.2 In the REPL pass resolvers: `.doc` uses anchors, `.kw` (prompt-toolkit backend) produces `kw:` keyword links, the plain backend inline code; use the docstring helper in the runtime fallback renderer on RF ≥ 7.5; extend `_pt/doc_viewer.py`'s anchor map for shifted headings and the TOC list; verify with tests in `tests/robotcode/repl/test_prompt_toolkit_interpreter.py` (`.kw` turns `[Set Log Level]` into a navigable link on RF ≥ 7.5 or with a fake Markdown `KeywordDoc`) and `test_doc_viewer.py` (anchor map for shifted headings and TOC), and manually with `.doc BuiltIn` and `.kw Log` on RF 7.5
- [ ] 4.3 In `packages/language_server/.../parts/http_server.py` pass a `link_resolver` producing `#slug` anchors (shared slug helper) to the Markdown view's `to_markdown` call; verify with a unit test calling the view's markdown builder on a fake Markdown `LibraryDoc` and asserting `[Set Log Level]` becomes `[Set Log Level](#set-log-level)`

## 5. Documentation and verification

- [ ] 5.1 Update `docs/03_reference/repl.md` (`.kw`/`.doc` output: argument descriptions, Returns/Raises, resolved links for Markdown-documented libraries) and the hover/signature-help mentions in `docs/index.md` if needed; verify with `npm run docs:build`
- [ ] 5.2 Run `hatch run lint:all` and `hatch run test:test` (full matrix incl. `rf75`) and confirm both pass with no regression-baseline changes
- [ ] 5.3 Manually check in VS Code on RF 7.5 and RF 7.4: hover on `Log`, signature help on `Log`'s `level`, `level=` completion, the BuiltIn library hover, a Collections keyword with an admonition; and in IntelliJ that the hover renders the description list
