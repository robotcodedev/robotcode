# Tasks: rebot-console-options

## 1. Prerequisites

- [x] 1.1 Confirm `support-rf75` tasks 1.1, 3.1, 3.2 and 3.4 are done; verify `grep -c 'alias="' packages/robot/src/robotcode/robot/config/model.py` is greater than 0, the `console` field type in `model.py` does not contain `skipped` (the word occurs only in Robot Framework's own help texts, e.g. `passed:failed:skipped`), `grep -n generate_rf_options CONTRIBUTING.md` finds the regeneration sequence, and `hatch env show` lists `test.rf75`

## 2. Generator and model

- [x] 2.1 Extend `scripts/generate_rf_options.py` with tool-prefixed override keys: `type_templates["rebot:console"] = 'Union[str, Literal["verbose", "quiet", "none"]]'`, `TOML_EXAMPLES["rebot:--console"] = ['console = "quiet"', '# custom console logger\nconsole = "path/to/Console.py:arg"']`, a two-step lookup (`f"{tool}:{name}"` then `name`) in `get_type` and `apply_toml_examples` (pass `tool` through from `generate()`), and build the `RebotOptions` dict from `RebotSettings._extra_cli_opts` plus `ConsoleType`/`ConsoleTypeQuiet` from `RebotSettings._cli_opts` when present; verify by running the generator in a scratch copy under RF 7.5 that `RebotOptions` gains exactly `console` and `quiet` with the rebot descriptions and under RF 7.4.2 the output is unchanged
- [x] 2.2 Leave `BaseOptions.add_options` unchanged (an `exclude` parameter was added first and removed by maintainer decision); the `[rebot]`-only handling of `console`/`quiet` lives in `rebot.py` (task 3.1)
- [x] 2.3 Regenerate `model.py` (`hatch run test.rf75:python scripts/generate_rf_options.py` from the repo root), run `hatch run lint:fix`, `hatch run create-json-schema` and `hatch run robotcode config info desc > docs/03_reference/config.md`; verify `git diff` of the generated region shows only the two new `RebotOptions` fields, the schema's `RebotProfile` lists `console` (string or `verbose`/`quiet`/`none`) and `quiet`, and `config.md` gains `## rebot.console` and `## rebot.quiet`

## 3. Runner

- [x] 3.1 In `packages/runner/src/robotcode/runner/cli/rebot.py` keep the `[rebot]` values of `console`/`quiet` across `add_options(profile)` (remember before, put back after, with a comment referencing RF's `get_rebot_settings`), and fix the `--dry` message to "Would execute rebot" (no version guard: `rebot` of RF < 7.5 rejects the options itself — maintainer decision, a guard was implemented first and removed); verify with `hatch run lint:all`
- [x] 3.2 Extend the reason text of `needs_rf_75` in `tests/robotcode/runner/cli/rf_markers.py` (added by `rf75-test-metadata`), and extend `tests/robotcode/runner/cli/test_robot_toml_options.py` (the rebot dry-run test module `support-rf75` task 3.3 created) with an in-process `CliRunner` (pattern of `tests/robotcode/runner/cli/discover/conftest.py`, invoked with `expect_ok=False` because `--dry` exits with RF's `INFO_PRINTED` code 251), isolated via `monkeypatch.chdir(tmp_path)`, `monkeypatch.setattr(robotcode.robot.config.utils, "get_user_config_file", lambda *a, **k: None)` (pattern of `tests/robotcode/repl/test_run.py`; otherwise the user config is created and injected) and `monkeypatch.delenv("ROBOT_OPTIONS"/"REBOT_OPTIONS", raising=False)`, running `robotcode --dry rebot output.xml` against a `robot.toml` in `tmp_path`; verify in every case `returncode == 251` and `"Would execute rebot with the following options" in stdout`; on RF ≥ 7.5 that `[rebot] console = "quiet"` yields `console = 'quiet'`, `[rebot] quiet = true` yields `quiet = True`, `[rebot] console = "path/to/Console.py:arg"` is forwarded, and a top-level `console = "dotted"` / `quiet = true` without `[rebot]` values yields neither; and on RF < 7.5 that the same configurations make `rebot` fail with its own option error (exit code `DATA_ERROR`, `[ ERROR ] option --console …`)
- [x] 3.3 Add model tests in `tests/robotcode/robot/config/test_model.py` (pattern of the `max-error-lines` tests) that `from_dict({"rebot": {"console": v}}, RobotConfig).rebot.build_command_line()` gives `["--console", v]` for `verbose`, `quiet`, `none` and `MyConsole`, `{"rebot": {"quiet": True}}` gives `["--quiet"]`, and a non-string `console` is rejected; and a profile test in `test_profile.py` loading `[rebot] console = "quiet"` from TOML; verify with `hatch run test:test -- tests/robotcode/robot/config tests/robotcode/runner/cli/test_robot_toml_options.py`

## 4. Verification

- [x] 4.1 Run `hatch run lint:all` and `hatch run test:test` (full RF matrix) and confirm both pass; verify the regeneration sequence from `CONTRIBUTING.md` reproduces `model.py`, the schema and `config.md` with no diff
- [x] 4.2 Manually run `robotcode rebot --output-dir out out/output.xml` with `[rebot] console = "quiet"` on RF 7.5 (no console output) and on RF 7.4 (rebot's own option error, exit code 252)
