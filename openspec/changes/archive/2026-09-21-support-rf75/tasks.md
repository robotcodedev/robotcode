# Tasks: support-rf75

## 1. Test matrix and environment

- [x] 1.1 Add `rf75` (`robotframework~=7.5.0`) to the `devel` and `test` matrices and their `matrix.rf.dependencies` overrides in `hatch.toml`, and to the `robot-version` list in `.github/workflows/build-test-package-publish.yml`; verify `hatch env show` lists `test.rf75` and `hatch run test.rf75:python -c "import robot; print(robot.__version__)"` prints `7.5`
- [x] 1.2 Update the environment lists: `AGENTS.md` (add `rf75` to the available `rf` envs, "40 environments" → 45, `5 × 8` → `5 × 9`), `CONTRIBUTING.md` examples (`test.rf74:test` → `test.rf75:test`, `devel.py3.12-rf74` → `-rf75`), `.github/copilot-instructions.md` and `.github/instructions/python-development.instructions.md` (`5.0–7.4` → `5.0–7.5`); verify `grep -rn "5\.0–7\.4\|40 environments\|5 × 8\|test\.rf74:\|-rf74:" AGENTS.md CONTRIBUTING.md .github/*.md .github/instructions/*.md` returns nothing and `rf75` appears in all four files
- [x] 1.3 Rewrite `test_original_variable_not_found_produces_recommendation` in `tests/robotcode/robot/test_robot_patching.py`: load `robot/variables/notfound.py` from its file into a private module with `importlib.util.spec_from_file_location` (RobotCode's patch rebinds the public name at import time of `library_doc`, so the public function cannot be used) and call its `variable_not_found` inside `pytest.raises(VariableError)`, asserting `Did you mean` and `${foobaz}` in the message and that the patched module's function is `_fast_variable_not_found`; verify with `hatch run test.rf50:test`, `test.rf70:test` and `test.rf75:test -- tests/robotcode/robot/test_robot_patching.py`
- [x] 1.4 Add a note to the docstring of `packages/robot/src/robotcode/robot/utils/robot_patching.py` that RF 7.5 (#5644) removed the recommendation cost on finder misses but `variable_not_found` still runs `RecommendationFinder` on real misses, so the patch stays; verify `hatch run lint:all`

## 2. Documentation extraction on RF ≥ 7.5

- [x] 2.1 In `packages/robot/src/robotcode/robot/diagnostics/library_doc.py` import `parse_docstring` from `robot.running.docstringparser` at module scope under `RF_VERSION >= (7, 5)` and add a helper that, given the libdoc `KeywordDoc` from `KeywordDocBuilder.build_keyword` and whether it is a resource keyword, returns the merged tags (`Tags.add(info.tags, remove_negated=is_resource)`) and the documentation text: `info.doc` when `info.args`, `info.returns` and `info.raises` are empty, otherwise the original text with a trailing `Tags:` line removed (the rule RF ≤ 7.4 applied); resource text is unescaped first, and the resource-file documentation is unescaped too; run the helper under `LOGGER.cache_only` so `parse_docstring`'s deprecation warning for a `Tags:` line without a preceding empty line never reaches the console; verify with unit tests in a new `tests/robotcode/robot/diagnostics/test_library_doc_docstrings.py` using a fixture library (docstrings ending `Tags: alpha, beta`, `Tags: robot:private`, an `Args:` docstring with a trailing `Tags:` line, a `Tags:` line without preceding empty line) and a fixture resource (`[Tags]    keep    stay` + `[Documentation]` ending `Tags: -keep, other`, a `\*escaped\*` doc) that `tags`, `is_private` and `doc` are identical on RF 7.4 and 7.5 for `get_library_doc` and `get_model_doc`, that the `Args:` text is present in `doc` on both versions, and (with `capfd`) that nothing is printed for the legacy layout on RF 7.5
- [x] 2.2 Apply the helper at the three keyword sites (library initializer, library keywords, resource keywords in `_build_resource_doc`; the two variables-file initializer sites stay unchanged); verify with tests that `get_library_doc_from_library(create_keywords=False)` and `get_resource_doc_from_resource` give the same tags/doc as the disk paths, that the RF keyword objects' `doc` and `tags` are unchanged after rendering, that nothing is printed on these paths either, and that `keyword_finder` still deprioritises a doc-declared `robot:private` keyword on RF 7.5
- [x] 2.3 Stop `_yield_type_info` at `info.is_recursive` on RF ≥ 7.5 and pass `libdoc.doc_format` to `RobotTypeDoc.for_type` on RF ≥ 7.5; verify with a test (skipped below Python 3.12 or RF 7.5) that writes a library with `type ID = int`, `type Tree = int | list[Tree]`, `type Pair[T] = tuple[T, T]` into `tmp_path`, calls `get_library_doc`, and asserts `errors is None`, the `integer`/`list`/`tuple` type docs exist and the `id` argument type is `ID`; and with a test that the `integer` TypeDoc of `BuiltIn` rendered on RF 7.5 contains no `[https://` link markup while on RF 7.4 the ROBOT conversion is unchanged
- [x] 2.4 Normalise return-type extraction at the library keyword site in `library_doc.py` (≈2426-2431) to the truthiness of `args.return_type` (`ArgumentSpec.from_robot_argument_spec` already does this); verify with tests that an unannotated keyword has `return_type is None` on RF 7.4 and 7.5, `-> int` gives `int`, and `-> None` gives `None` on RF 7.5
- [x] 2.5 Keep `markdown` optional (maintainer decision, design D3): no dependency in `packages/robot/pyproject.toml`, no entry in `bundled_requirements.txt`; add `markdown>=3.6` as a development dependency to the hatch default environment in `hatch.toml`; verify with a test (skipped below RF 7.5) that `get_robot_library_html_doc_str("Collections", …)` succeeds, that with `sys.modules["markdown"] = None` (fresh interpreter) it raises RF's `DataError` containing `Markdown format requires` (the existing web-view error page shows that message), and that `hatch run build:install-bundled-editable` does not produce `bundled/libs/markdown/`
- [x] 2.6 Document in `docs/02_get_started/index.md` (Requirements → Optional Python Packages) that `docutils` and `markdown` are optional in Robot Framework, are neither dependencies of RobotCode nor bundled, what each is needed for and what happens without it, and that users install them into their project environment themselves — without an installer-specific command such as `pip install`, because projects use different package and environment managers; verify with `npm run docs:build`

## 3. `robot.toml` model and generator

- [x] 3.1 Repair `scripts/generate_rf_options.py`: emit `alias="<kebab>"` / `alias="extend-<kebab>"` for fields containing `_`, move the `max-error-lines = "NONE"` example into `TOML_EXAMPLES` and change the tag-pattern examples there to `foo AND bar*`, change the `console` type override to `Union[str, Literal["verbose", "dotted", "quiet", "none"]]` with an additional custom-console example, build the option dicts explicitly so that `CommonOptions` comes from `RobotSettings._cli_opts` without `ConsoleType`/`ConsoleTypeQuiet` and `RobotOptions` from `RobotSettings._extra_cli_opts` with them (no `RebotOptions` change), and fix the `RobotExtendOptions` generation that passes `tool="rebot"` (its docstrings wrongly say "option of _rebot_"); verify by running the generator in a scratch copy against RF 7.4.2 (diff to the committed `model.py`: only the `console` type and the examples) and against RF 7.5 (additionally the `MARKDOWN` values in the libdoc `format`/`doc_format` Literals and RF 7.5 docstring wording; no class gains or loses fields)
- [x] 3.2 Regenerate `packages/robot/src/robotcode/robot/config/model.py` with the generator under RF 7.5 (`hatch run test.rf75:python scripts/generate_rf_options.py` from the repo root), run `hatch run lint:fix`, then `hatch run create-json-schema` and `hatch run robotcode config info desc > docs/03_reference/config.md` (this also picks up the pending `max-error-lines`/`wrapper` drift of the committed file); verify the schema keeps kebab-case property names (`console-colors`, `doc-format`), lists `MARKDOWN` for `libdoc.doc-format`/`libdoc.format`, accepts a string for `console` and no longer lists `skipped`, has no `rebot.console`, and that `git diff --stat` touches only the generated region, the schema and `config.md`
- [x] 3.3 Add config-model tests under `tests/robotcode/robot/config/` that `console = "MyConsole.py:arg"` validates and builds `--console MyConsole.py:arg`, that the `console` field type and the schema no longer contain `skipped`, and that `[libdoc] format = "MARKDOWN"` / `doc-format = "MARKDOWN"` validate and build `--format MARKDOWN` / `--docformat MARKDOWN`; add a `CliRunner` test under `tests/robotcode/runner/cli/` that runs `robotcode --dry -p <profile> rebot output.xml` with a top-level `console = "dotted"` in a `robot.toml` in `tmp_path` and asserts `console = 'dotted'` does not appear in the dry-run output, and one (skipped below RF 7.5) that `robotcode libdoc` with `format = "MARKDOWN"` writes a `.md` file; verify with `hatch run test:test -- tests/robotcode/robot/config tests/robotcode/runner`
- [x] 3.4 Document the regeneration sequence (generator with RF-7.5 interpreter → `lint:fix` → `create-json-schema` → `robotcode config info desc > docs/03_reference/config.md`) in `CONTRIBUTING.md` next to the other maintainer scripts; verify the commands as written reproduce the committed files with no diff

## 4. Built-in variables, Markdown resources, grammar

- [x] 4.1 Add `&{TEST_METADATA}` to `BUILTIN_VARIABLES` in `packages/robot/src/robotcode/robot/utils/variables.py` under `RF_VERSION >= (7, 5)` (the lookup ignores the identifier, so `${TEST_METADATA}` resolves too, as `${SUITE_METADATA}` does today); verify with a plain analyzer test in `tests/robotcode/robot/diagnostics/` over an inline document with a test-level `[Metadata]` and `Log    ${TEST_METADATA}`: on RF ≥ 7.5 zero diagnostics, on RF < 7.5 one error whose message starts with `Setting 'Metadata' is not allowed` plus a variable-not-found for `${TEST_METADATA}`; a semantic-analyzer snapshot test (`skipif RF_VERSION < (7, 5)`, pattern of `test_semantic_analyzer`) showing `[Metadata]` as a setting name; and a completion test that `${TEST_METADATA}` is offered on RF ≥ 7.5 (no file under `data/tests/versions/`, to avoid new baselines in every RF version directory)
- [x] 4.2 Add `.md`/`.markdown` to `imports_manager.RESOURCE_EXTENSIONS` and `library_doc.ALLOWED_RESOURCE_FILE_EXTENSIONS` under `RF_VERSION >= (7, 5)` and make `get_robot_library_html_doc_str` raise a clear error ("Libdoc does not support Markdown resource files") for them instead of handing them to `LibraryDocumentation`; verify with tests (skipped below RF 7.5) that `Resource    keywords.md` produces no invalid-extension diagnostic, that resource-path completion lists `.md` files, that the HTML request raises the clear error, and that on RF 7.4 the diagnostic message starts with `Invalid resource file extension '.md'` (do not compare the full message, its extension list has set order)
- [x] 4.3 Edit the section-header regexes of `syntaxes/robotframework.tmLanguage.json` and `syntaxes/robotframework-repl.tmLanguage.json`: add `cas de test`, keep `unités de test`, add the Arabic terms from `robot.conf.languages.Ar` (settings, variables, test cases, tasks, keywords, comments, documentation setting); extend `scripts/generate_tmlanguage.py` to include `Language.deprecations` keys without running it; note in `CONTRIBUTING.md`'s `generate-tmlanguage` bullet that generator and template are out of sync with the committed grammar (hand-edited in `6fad9b16`) and must not be run blindly, and mention the deliberate hand edit in the commit body; verify that both JSON files still parse (`python -c "import json; json.load(open(...))"`) and by tokenizing `*** Unités de test ***`, `*** Cas de test ***` and the Arabic headers with a scratch script against the grammar (or `vscode-tmgrammar-test`)

## 5. Baselines, documentation and verification

- [x] 5.1 Create the RF 7.5 regression baselines after tasks 2–4 the established way: copy `tests/robotcode/language_server/robotframework/parts/_regtest_outputs/rf74/` to `rf75/`, run `hatch run test.rf75:test -- tests/robotcode/language_server/robotframework/parts` against the copy and review every regtest failure diff — expected are only `line:` shifts inside RF's own `BuiltIn.py`/`Collections.py` (goto definition/implementation, references), anything else must be understood (intended change) or fixed (regression) before continuing — then accept with `hatch run test.rf75:test-reset tests/robotcode/language_server/robotframework/parts`; verify `git status` shows only the new `_regtest_outputs/rf75/` tree and a subsequent `hatch run test.rf75:test` is green; document this copy → run → review → reset procedure in `CONTRIBUTING.md` next to the RF matrix section so the next version bump follows it
- [x] 5.2 Add a note to `docs/03_reference/analyzing-results.md` that an `output.xml` written by RF 7.5 with test-level `[Metadata]` cannot be read by RF < 7.5 (`robotcode results` and `rebot`); verify with `npm run docs:build`
- [x] 5.3 Run `hatch run lint:all` and `hatch run test:test` (full RF matrix incl. `rf75`) and confirm both pass; run `hatch run devel.py3.14-rf75:test` once for the newest Python
- [x] 5.4 Manually check on RF 7.5 in VS Code: hover on `Log` shows Markdown documentation with the `Args:` text present and no `Tags:` line, "show documentation" for `Collections` renders, a `robot.toml` with `console = "path/to/Console.py"` runs, `[Metadata]` in a test shows no diagnostics, and `printf '.doc BuiltIn\n' | robotcode repl` prints the documentation without an error
- [x] 5.5 Re-verify the debugger on RF 7.5 after the code changes: run the same scripted `robotcode robot-debug --plain --no-history --break <suite>:<line> -- <suite dir>` session (commands `.where`, `.vars`, `.print ${var}`, `.print $var.upper()`, `.next`, `.step` into a resource keyword, `.up`, `.down`, `.return`, `.set ${var} value`, a keyword call at the prompt, `.catch all`, `.continue` through a failing test, plus a test whose `[Timeout]` expires while stopped) under `hatch run test.rf74:…` and `hatch run test.rf75:…` and confirm the transcripts differ only by `&{TEST_METADATA}`; then check in VS Code on RF 7.5: line and conditional breakpoints, step in/over/out through a resource keyword, the Variables view (Local/Test/Suite/Global incl. `&{TEST_METADATA}` for a test with `[Metadata]`), evaluate in the debug console, the exception breakpoint filters (failed keyword, uncaught failed keyword, failed suite — "Failed Test" is a known defect on every RF version, owned by `debugger-dap-e2e-tests`), and a debug run started from the test explorer; verify no traceback appears in the debug console or the RobotCode output channel
- [x] 5.6 Re-verify the CLI tools on RF 7.5: on one sample project run `robotcode robot`, `rebot`, `libdoc BuiltIn out.html`, `libdoc BuiltIn out.json`, `libdoc <resource> out.html`, `testdoc`, `discover all|tests|suites|tags|files|info`, `results summary|show|log|stats|diff`, `analyze code`, `config show`, `profiles list` and a piped `repl` session (keyword execution, `Import Resource`, `.imports`, `.kw Log`, `.doc Collections`) under the `test.rf74` and `test.rf75` environments; verify identical exit codes and identical output apart from version strings, timings and RF 7.5's Testdoc deprecation warning, and that `hatch run test.rf75:test -- tests/robotcode/runner tests/robotcode/repl tests/robotcode/debugger tests/robotcode/analyze tests/robotcode/cli` is green

## 6. Upstream

- [x] 6.1 Track the upstream-related findings in RobotCode's own issue tracker instead of reporting them right away (maintainer decision): (a) Markdown/reST suites and resources are not supported for now, including Robot Framework's block-relative line numbers in `.robot.md`/`.robot.rst` and the open question whether to report them to Robot Framework — https://github.com/robotcodedev/robotcode/issues/651; (b) a doc-declared `Tags: robot:private` being ignored by Robot Framework's runtime is identical on RF 7.4 and 7.5 and needs no report; the related idea of hiding private keywords of other files in keyword completion, switchable — https://github.com/robotcodedev/robotcode/issues/652; verify by linking both issue URLs in `design.md` under Open Questions
