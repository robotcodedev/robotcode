# Tasks: debugger-dap-e2e-tests

## 1. Harness

- [ ] 1.1 Add `tests/robotcode/debugger/conftest.py` with a suite builder writing fixture suites and a resource file to `tmp_path`, a fixture starting `sys.executable -m robotcode.cli debug --tcp 127.0.0.1:<find_free_port()> --no-debugpy -- --output-dir <tmp> --log NONE --report NONE <suite dir>` with captured output and teardown by `kill()` (a stopped server ignores `SIGTERM`), and an async client fixture built on `robotcode.debugger.launcher.client.DAPClient` with a recording parent protocol that stores every event, answers events whose body has `synced: true` with a `robot/sync` request, and offers `wait_for_event(name, timeout)`; the fixture performs `initialize`, the test's breakpoint requests, `configurationDone`, `attach` (the launcher's order); verify with a smoke test that a run without breakpoints delivers `robotExited` with the expected exit code and `terminated` within 10 s (i.e. no 15 s sync stalls), and that the server process has exited after the test on Linux, Windows and macOS (CI)
- [ ] 1.2 Attach the recorded DAP transcript and the server output to the pytest report on failure or timeout; verify by forcing a timeout in a throw-away test and checking the report contains the transcript

## 2. Defect fixes (each verified by its scenario in group 3)

- [ ] 2.1 Exception filter ids: in `packages/debugger/src/robotcode/debugger/debugger.py` accept `failed_test` in `set_exception_breakpoints`, pass `{"failed_test"}` from `end_test`, drop the `""` id, and validate ids against the filters declared in `default_capabilities.py`; verify with the "Failed test" scenario (response `verified: true`, one stop with text starting `Test failed`)
- [ ] 2.2 Plain `filters`: build exception-breakpoint entries from `filters` as well as from `filter_options` (union, no condition for plain filters); verify with the scenario sending only `filters`, and that a request with both lists empty still disables every stop
- [ ] 2.3 Hit condition: stop when the hit count equals the requested number (`==` instead of `!=` in `process_start_state`); verify with the hit-condition scenario (condition `2` on a line hit four times stops exactly once, in the second iteration)
- [ ] 2.4 Vanished or detached client (design D6): make `on_debugger_send_event` in `packages/debugger/src/robotcode/debugger/server.py` wait for `robot/sync` only while a client is connected and attached, let `connection_lost` detach like `disconnect` without `terminateDebuggee` (`attached = False`, `continue_all()`), keep the server listening, and clear `_disconnected_event` in `connection_made` so the end-of-run wait for `disconnect` works for a later client; describe the handshake (synced events, `robot/sync`, 15 s timeout, no wait without a client) in the docstrings of `on_debugger_send_event` and `_robot_sync`; verify with the scenarios of "A vanished or detached client never ends or stalls the run" and "A new client can attach to the session"

## 3. Scenarios

- [ ] 3.1 Lifecycle and breakpoints: run without breakpoints, `terminate` at a stop, verified line breakpoint, conditional breakpoint inside a `FOR` loop, hit condition, log point with Robot Framework variable syntax; verify each spec scenario of "Session lifecycle", "Synced events" and "Breakpoints stop the run at the requested line" with assertions on events and top-frame location
- [ ] 3.2 Inspection and stepping: stack frames inside a resource keyword, scopes in a test body (`Local`/`Suite`/`Global`) and inside a user keyword (`Local`/`Test`/`Suite`/`Global`) with expandable list and dictionary variables, `setVariable` with the value `'changed'` visible in a following `Log`, `next`/`stepIn`/`stepOut`/`continue`/`pause`; verify the scenarios of "Stack, scopes and variables" and "Stepping"
- [ ] 3.3 Evaluation: a variable in `watch` and `hover`, a Python expression (`$local.upper()`) in `watch`, a keyword call in the `repl` context whose message arrives as output, an unknown variable (`<undefined>` in `watch`, unsuccessful response in `repl`, session stays stopped), `completions` offering a keyword and a variable; verify the scenarios of "Evaluation and completion in the paused context"
- [ ] 3.4 Exception filters, detaching and timeouts: each of the four filters alone as `filterOptions`, the uncaught filter as plain `filters`, the default without a request, an empty request, a caught failure (`TRY/EXCEPT` and `Run Keyword And Expect Error`) with only the uncaught filter, `disconnect` at a stop, socket closed at a stop, a second client attaching after each of the two (long-running fixture test, new breakpoint, stack trace, `continue`, run-end events, answered `disconnect`), a test timeout expiring while stopped; verify the scenarios of "Exception filters stop on failures", "A vanished or detached client never ends or stalls the run" and "A new client can attach to the session"

## 4. Verification

- [ ] 4.1 Run `hatch run lint:all` and `hatch run test:test -- tests/robotcode/debugger` on the full RF matrix and confirm the suite is green and adds no more than about 15 s per environment; run it three times in a row on one environment to check for flakiness, and confirm with `pgrep -f "robotcode.cli debug"` that no server process is left behind
- [ ] 4.2 Check the fixes in VS Code: the "Failed Test" exception breakpoint stops at the end of a failing test, a hit-count breakpoint `2` stops in the second iteration, and after closing the window during a paused session the `robotcode … debug` process finishes the run and exits on its own; note the result in the commit body
- [ ] 4.3 Update `support-rf75` task 5.5 (or its successor for the next RF version) to reference this suite instead of the manual DAP checks, keeping only the editor-specific manual checks; verify the cross-reference exists
