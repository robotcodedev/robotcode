# Tasks: markdown-suites

## 1. Extraction helper

- [ ] 1.1 Create `packages/robot/src/robotcode/robot/utils/embedded_code.py` with the extension constants (`REST_EXTENSIONS`, `MARKDOWN_EXTENSIONS`), Markdown extraction mirroring RF's `MarkdownParser` (fence regexes; the info string's first word must be exactly `robotframework`/`robot`, case-insensitive, later words ignored; same-character/at-least-same-length closing; unclosed trailing block kept; per-block dedent) and reST extraction via docutils with RF's directive replacement and a recording code-block directive (blocks from included files ignored; `docutils` imported lazily inside the reST function, as RF's `RestParser` does, so a missing package yields a typed "docutils missing" result and can be simulated in tests), returning the file-geometry text with per-line margins, RF's concatenated text and the extracted→file line table, plus module-scope constants (`MARKDOWN_SUPPORTED = RF_VERSION >= (7, 5)`, `REMAP_RF_LINE_NUMBERS = True`); verify with `tests/robotcode/robot/utils/test_embedded_code.py` (fences ``` and ~~~, longer fences, `robotframework title=x` opens and `robotframework{.x}` does not, non-robot fences ignored, nested fence inside a non-robot fence opens a block like RF, indented fence with margin table, unclosed block, empty closed block yielding one empty line, CRLF and BOM fixtures written with `write_bytes`, tabs, file without blocks) and parity tests (`skipif RF_VERSION < (7, 5)` for Markdown, `importorskip("docutils")` for reST) asserting the concatenated text equals `MarkdownParser()._read_markdown_data(FileReader(path))` / `read_rest_data` for every fixture, all written to `tmp_path`

## 2. Language server core

- [ ] 2.1 Extend `document_cache_helper.__get_document_type` to compound suffixes and RF-version rules (`.robot.md` GENERAL on RF ≥ 7.5, `.robot.rst` GENERAL, `.md`/`.markdown` RESOURCE on RF ≥ 7.5, `.rst`/`.rest` RESOURCE, `__init__.md`/`__init__.rst` INIT by RobotCode's own decision, `__init__.robot.md` GENERAL), make `get_tokens` accept them, tokenise the extracted text in the three tokenisers adding the margin to `Token.col_offset`, cache the extraction on the document and expose the block table; import the extension constants in `library_doc.py` and `imports_manager.py` from `embedded_code.py` and delete `imports_manager`'s unused `REST_EXTENSIONS`; verify by extending `tests/robotcode/robot/diagnostics/test_document_cache_helper.py` with typing cases per RF version and a position test asserting that a test in an indented fence has statement `lineno`/`col_offset` equal to its file line/column
- [ ] 2.2 Make `documents_manager.detect_language_id` match compound suffixes, let the LS `didOpen`/`get_or_open_document` prefer the extension-detected language id when it is not unknown, extend `LanguageDefinition` extensions and `file_extensions` in `robotframework/protocol.py` with `md`, `markdown`, `rst`, `rest`, and the workspace preload in `parts/robot_workspace.py` with `*.robot.md` (RF ≥ 7.5) and `*.robot.rst`; verify with a namespace test that a suite importing `keywords.md` (RF ≥ 7.5) and `keywords.rst` (docutils) has no `ResourceEmpty`/`KeywordNotFound` diagnostics and that the keyword definition points at the file line inside the fence, that a document opened with language id `markdown` and a `.robot.md` name is analysed, that a `.robot.rst` document analysed with `monkeypatch.setitem(sys.modules, "docutils", None)` yields exactly one docutils diagnostic and no logged exception, and that workspace preload of a `tmp_path` folder containing `README.md` and `sample.robot.md` opens only the latter
- [ ] 2.3 Withhold whole-document edits for embedded documents: `parts/formatting.py` returns `None`, EOF-appending quick fixes and text-based refactors in `code_action_quick_fixes.py`/`code_action_refactor.py`/`code_action_helper_mixin.py` are skipped, `robocop_diagnostics.py` passes the extracted text; verify with a formatting test asserting `None` for a `.robot.md` document and a quick-fix test asserting no EOF-appending action is offered
- [ ] 2.4 Add LS regression data files `tests/robotcode/language_server/robotframework/parts/data/tests/embedded_markdown.robot.md` (skipped below RF 7.5) and `embedded_rest.robot.rst` (all versions, skipped without docutils) with non-indented fences and marker lines at column 0 inside the code blocks (the marker parser expects `# ^^^ name` at column 0; the indented-fence case is covered by the 2.1 unit test), and a test module `test_embedded.py` in the pattern of `test_hover.py`/`test_goto_definition.py` covering hover, definition, references, semantic tokens and document symbols; record baselines after 2.1–2.3 with `hatch run test.<rf>:test-reset tests/robotcode/language_server/robotframework/parts/test_embedded.py` for every matrix env (`rf50`…`rf75`; the Markdown file produces baselines only under `rf75/`); verify `git status` shows only the new baseline files, they show file-accurate positions, and `hatch run test:test -- tests/robotcode/language_server` is green

## 3. CLI

- [ ] 3.1 In `packages/runner/src/robotcode/runner/cli/discover/discover.py` remap `lineno`/`range`/ids of tests and suites from embedded sources through the helper's line table (reading via `FileReader` so `--read-from-stdin` content applies) behind `REMAP_RF_LINE_NUMBERS`, and list `*.robot.rst` and (RF ≥ 7.5) `*.robot.md` in `discover files` (update its docstring and regenerate `docs/03_reference/cli.md` with `hatch run create-cmd-line-docs`, keeping only the `discover files` hunk); verify with acceptance tests in `tests/robotcode/runner/cli/discover/` (fixture suites `suites/embedded/sample.robot.md` and `sample.robot.rst`) that `lineno` equals the file line of the test name, that stdin content with an extra prose line shifts it accordingly, and that `files` lists the embedded suites but not `README.md`
- [ ] 3.2 Extend `analyze code` (`robot_framework_language_provider.py`, `code_analyzer.py`) to scan `*.robot.rst`/`*.robot.md` in workspace folders and to analyse explicitly passed Markdown/reST files; verify with tests under `tests/robotcode/analyze/code/` that a `tmp_path` project with a `.robot.md` suite importing `keywords.md` and a `README.md` reports zero diagnostics and exactly one analysed file on RF 7.5 (the README is not scanned), and that `analyze code keywords.md` reports one analysed file

## 4. Clients

- [ ] 4.1 VS Code: add the setting `robotcode.embeddedFilePatterns` (default `["**/*.robot.md", "**/*.robot.rst"]`) feeding pattern document selectors in `languageclientsmanger.ts`, extend `fileExtensions` with `robot.md`/`robot.rst`, replace language-id gates with "language id OR pattern" in `languageclientsmanger.ts`, `testcontrollermanager.ts`, `debugmanager.ts`, `languageToolsManager.ts`, `keywordsTreeViewProvider.ts`, update `package.json` activation events, command enablement and menus; verify with `npm run lint`, `npm run compile` and manually: open `sample.robot.md` (Markdown preview works, hover/goto/semantic tokens inside fences, test explorer items at file lines, Run Current File enabled)
- [ ] 4.2 IntelliJ: add `<fileNamePatternMapping patterns="*.robot.md;*.robot.rst" serverId="RobotCode" languageId="robotframework"/>` to `plugin.xml` and pattern checks next to the file-type gates in `RobotCodeTestManager.kt` (optionally `EditorNotificationProvider.kt`); verify with `(cd intellij-client && ./gradlew test && ./gradlew verifyPlugin)` and manually that a `.robot.md` file gets hover and gutter run icons at file lines

## 5. Documentation and verification

- [ ] 5.1 Add a documentation page on Markdown/reST suites and resources (which files count, the VS Code setting, docutils not bundled, RF's behaviour for indented/nested fences, known limitations: no formatting/EOF quick fixes, `__init__.robot.md` is a suite that RF skips in directory runs, `__init__.md`/`__init__.rst` are init files for RobotCode but for RF only with a configured extension, debugger lines block-relative until `embedded-suites-debugging`), update `docs/03_reference/discovering-tests.md` (`lineno` semantics, `files` list); verify with `npm run docs:build`
- [ ] 5.2 Run `hatch run lint:all` and `hatch run test:test` (full matrix incl. `rf75`) and confirm both pass
