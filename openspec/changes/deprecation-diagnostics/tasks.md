# Tasks: deprecation-diagnostics

## 1. Prerequisites and rule module

- [ ] 1.1 Confirm `support-rf75` is applied (`rf75` env exists) and add `deprecations: Dict[str, Tuple[str, Optional[str]]]` to the `Languages` Protocol in `packages/robot/src/robotcode/robot/utils/stubs.py`; verify `hatch run lint:all`
- [ ] 1.2 Create `packages/robot/src/robotcode/robot/diagnostics/deprecations.py` with pure functions `find_deprecated_header(token, languages)`, `find_deprecated_setting(token, languages)` (RF's `title()`/whitespace/bracket normalisation, skipping tokens that carry an RF error), `find_tags_line_without_empty_row(documentation_node)` (RF's `header_re` and `can_start` rule over the rows of a `[Documentation]`), and `replacement_for(token_text, new_term)` keeping header decoration and brackets; register `DEPRECATED_TRANSLATION` and `TAGS_WITHOUT_EMPTY_ROW` in `errors.py`; verify with pure-function tests in a new `tests/robotcode/robot/diagnostics/test_deprecation_diagnostics.py` (`*** unités  de test ***` → `Cas de test` match and `*** Cas de test ***` replacement, `[Tagit]` → `[Tunnisteet]`, `Testin Tagit`, a custom language file with `deprecations` including a `(new, None)` entry whose message has no "since" clause, a token with `error` set returns no match, `Tags:`/`Tags::`/`**Tags:**` headers, `Tags:` as the first documentation line not reported, empty row before `Tags:` not reported)

## 2. Analyzer wiring

- [ ] 2.1 Wire the rules into `NamespaceAnalyzer` and `SemanticAnalyzer` (`visit_SectionHeader`, a generic check in `visit()` over statements whose first token type is in `Token.SETTING_TOKENS`, and `visit_DocumentationOrMetadata` for keyword documentation via the node stack), emitting `DeprecatedTranslation` (INFORMATION, Deprecated tag, RF's wording "<Kind> '<old>' has been deprecated since Robot Framework <version>. Use '<new>' instead.", without the "since" clause when the version is `None`) and `TagsWithoutEmptyRow` (WARNING, Deprecated tag) under `RF_VERSION >= (7, 5)`; verify with tests in `test_deprecation_diagnostics.py` parametrised over both analyzers (extend `analyzer_factory` in `tests/robotcode/conftest.py` with `languages: Optional[Languages] = None` and `analyzer_cls` parameters and give `parse_robot` a `lang=` keyword, following `_prepare_analyzer` in `test_variable_pipeline_comparison.py`) that the French header, `Testin Tagit` and `[Tagit]` each yield exactly one diagnostic with the expected code, severity, tag, range and replacement, that a custom language file written to `tmp_path` deprecating a non-tag setting (a translated `Documentation`) yields one diagnostic under both analyzers, that a legacy `Tags:` layout in a resource keyword yields the warning on the `Tags:` row and the correct layout none, that suite-level documentation is not checked, that nothing is reported on RF < 7.5 (inverse gate), and that `# robotcode: ignore[DeprecatedTranslation]` suppresses the diagnostic through `DiagnosticsModifier`
- [ ] 2.2 Run `hatch run test:test -- tests/robotcode/robot/diagnostics` and confirm the analyzer snapshot and pipeline-comparison tests are unchanged (no `Language:` fixtures exist, so no snapshot churn is expected)

## 3. Language server

- [ ] 3.1 Add `code_action_replace_deprecated_translation` / `resolve_…` and `code_action_insert_empty_row_before_tags` / `resolve_…` to `packages/language_server/.../parts/code_action_quick_fixes.py` following the collect/resolve pattern, recomputing the header/setting replacement from the token and `namespace.languages.deprecations`, and inserting `' ' * continuation_col + '...' + line ending` before the `Tags:` row; verify by extending `tests/robotcode/language_server/robotframework/parts/test_code_action_quick_fixes_model.py` with pure-function tests for both edits (decoration preserved; indentation from the CONTINUATION token, e.g. column 4 → `    ...`) and resolve tests in the existing `object.__new__` + mock style
- [ ] 3.2 In `parts/completion.py` mark deprecated setting labels in the suite, test-case and keyword settings builders with `CompletionItemTag.DEPRECATED`, `deprecated=True` and a later sort key on RF ≥ 7.5, and let the per-language branch (used when `filterDefaultLanguage` is on) additionally offer each `Language.deprecations` key whose replacement is a setting, tagged the same way; verify with a unit test instantiating the completion collector with a stub namespace exposing `languages` (pattern of `test_unused_variable_diagnostics.py`), once with `CompletionConfig(filter_default_language=False)` and once with `True`, that `[Tagit]` carries the tag and sorts after `[Tunnisteet]`, which carries none; skip below RF 7.5

## 4. Documentation and verification

- [ ] 4.1 Add the two codes to the deprecation list in `docs/03_reference/analyzing-code.md` (and a row in `docs/03_reference/diagnostics-modifiers.md` if the common-codes table is kept), note in `docs/02_get_started/configuration.md` that deprecated translations are flagged on RF ≥ 7.5 with a quick fix, add a sentence to `docs/03_reference/analyzing-results.md` and a comment line to the `--include`/`--exclude` entries of the generator's `TOML_EXAMPLES` (`# RF 7.5: operators must be surrounded with spaces or tag names be lower case; '&' is deprecated`) that RF 7.5 warns about `a&b` and `FooANDbar` and RF 8 will change them, then regenerate `model.py`, `docs/public/schemas/robot.toml.json` and `docs/03_reference/config.md` with the sequence `support-rf75` documents; verify with `npm run docs:build` and that the regeneration sequence reproduces the three committed files with no diff
- [ ] 4.2 Run `hatch run lint:all` and `hatch run test:test` (full matrix incl. `rf75`) and confirm both pass; manually check in VS Code on RF 7.5 that the header diagnostic shows strike-through, the quick fix replaces the term, the `Tags:` fix inserts the row, and `[Tagit]` appears struck through in completion; check the same completion rendering once in IntelliJ
