# Tasks: doc-cli

## 1. Prerequisites and decisions

- [ ] 1.1 Confirm that `analyze-config-in-robot` is applied: `AnalyzeConfig` (the analysis part of `[tool.robotcode-analyze]` with `to_workspace_analysis_config()`) and `get_analyze_config` are importable from `robotcode.robot.config.analyze_config` (`packages/robot/src/robotcode/robot/config/analyze_config.py`), `merge_variable_and_path_options` from `robotcode.robot.config.utils`, and `load_robot_config_from_path` reads the section without `extra_tools`; note how its Q4 was decided, since with (b) the command passes `extra_tools=BUILTIN_TOOL_CONFIG_CLASSES` (D7); verify with `hatch run python -c "from robotcode.robot.config.analyze_config import get_analyze_config; from robotcode.robot.config.model import RobotConfig; from robotcode.robot.config.utils import merge_variable_and_path_options; print(get_analyze_config(RobotConfig()).to_workspace_analysis_config())"` and `hatch run test:test -- tests/robotcode/robot/config tests/robotcode/analyze` green
- [ ] 1.2 Get the maintainer's answers to Q1–Q8 of design.md, write them into design.md (replace each "Recommended" by the decision), add the requirements that Q4 and Q6 introduce to `specs/documentation-cli/spec.md` when they are confirmed, and adjust the conditional tasks below; verify with `openspec validate doc-cli`

## 2. Converter corrections and scope value

- [ ] 2.1 In `packages/robot/src/robotcode/robot/utils/markdownformatter.py` escape the `|` characters left in a `TableFormatter` cell after its inline formatting and write link targets of `LinkFormatter` (`[url|text]` links and bare URLs) with `#` instead of `\#`, leaving link texts, other text and `LibraryDoc._link_inline_links` as they are; verify with a new `tests/robotcode/robot/utils/test_markdownformatter.py` (`| x | (Foo|Bar) |` keeps two cells, a cell with a `[http://example.com|text]` link keeps the link, `[http://example.com/x.html#frag|docs]` → `[docs](http://example.com/x.html#frag)`, a bare URL with `#`, `\#` in plain text unchanged, a `[#Section|text]` link), an RF < 7.5 test in `tests/robotcode/robot/diagnostics/test_library_doc_rendering.py` that the `Should Match Regexp` hover keeps the cell count of its header row, and unchanged golden strings in that file; before the change, save `KeywordDoc.to_markdown()` of BuiltIn `Log` on RF 6.1 and RF 7.5 with a throwaway script for the comparison in 4.4
- [ ] 2.2 In `get_library_doc_from_library` (`packages/robot/src/robotcode/robot/diagnostics/library_doc.py`) store `lib.scope.name` on RF ≥ 7.0 and `str(lib.scope)` below, chosen once at module import; drop `_without_version_specifics` from `test_library_doc_rendering.py`; verify that `get_library_doc("Collections").scope == "GLOBAL"` on every RF environment and that `hatch run test:test -- tests/robotcode/robot tests/robotcode/language_server/robotframework/parts/test_hover.py` shows no baseline diffs

## 3. Anchors

- [ ] 3.1 If Q1 is (a) (recommended): make `slugify` in `packages/robot/src/robotcode/robot/utils/markdown_docs.py` follow GitHub's heading rule (lower case; letters, digits, `-`, `_` and spaces kept; spaces to `-`), add a stateful `Slugger` that numbers repeated slugs `-1`, `-2` in call order, and let `LibraryDoc._link_inline_links`/`_create_toc` use `slugify` instead of their inline copy; with (b) or (c) implement that rule in `Slugger` only and leave `slugify` unchanged; verify with `tests/robotcode/robot/utils/test_markdown_docs.py` (punctuation, `${arg}` names, underscores, non-ASCII letters, numbering, the existing `string-representations` case; with (a) the `Library *BuiltIn*` expectation of `test_slugify_matches_the_anchors_of_the_robot_format_rendering` becomes `library-builtin`), unchanged golden strings in `test_library_doc_rendering.py`, and with (a) a test that the RF 7.5 DateTime library hover's TOC links `` `TODAY` and `NOW` `` to `#today-and-now`
- [ ] 3.2 Build the anchor map of `packages/repl/src/robotcode/repl/_pt/doc_viewer.py` with `Slugger` so repeated headings number like the renderer; verify with a `tests/robotcode/repl/test_doc_viewer.py` case with two headings of the same text and the existing anchor-map tests (with Q1 (a) the expectation of `test_build_anchor_to_line_map_strips_emphasis_for_matching` becomes `library-builtin`)

## 4. Canonical renderer

- [ ] 4.1 Create `packages/robot/src/robotcode/robot/diagnostics/documentation_markdown.py` with `DocumentationMarkdown`, `OutlineEntry` (`kind` in `document`/`section`/`init`/`keyword`/`type`, `name`, `level`, `anchor`), `render_documentation(doc, *, keywords=None, import_args=(), project_root=None, document_links=None)`, `short_doc` and `find_entry` (D1): title, metadata list (Type, Version, Scope, Source relative to `project_root` or omitted, Import arguments), `Introduction`, `Importing` (initializer with arguments or load errors, errors first), `Keywords`, `Data types`, keywords and types sorted by `name.lower()`, private keywords skipped; verify with a new `tests/robotcode/robot/diagnostics/test_documentation_markdown.py` on all RF environments: fixture library and resource written to `tmp_path` (section order and headings, no `Importing` without initializer arguments, `Zeta Kw`/`Alpha Kw` order, private keyword absent on RF ≥ 6.0, `.robot` suite file documented with its keywords, `NoSuchLib` with the import error in `Importing` and no `Keywords`)
- [ ] 4.2 Render keyword, initializer and type entries (D2): the Libdoc-style argument list for every keyword (no table; `*`/`**`; named-only and positional-only in italics), `**Returns**`, `**Raises**`, `**Tags**`, the documentation with headings shifted to level 4 and capped at 6; type entries with kind, documentation, allowed values, dictionary structure, converted types and "used by" links; argument and return types linked through `ArgumentInfo.type_docs`/`KeywordDoc.return_type_docs`; verify with golden strings for a Robot-format and a Markdown-format fixture library in `tmp_path`, an `Enum`/`TypedDict` fixture on RF ≥ 6.1 (links in both directions), `Log` on RF ≥ 7.5 (descriptions, no `Args:`) and `Should Be Equal` on RF < 7.5 (arguments listed once, no table)
- [ ] 4.3 Convert every documentation format as in D4 (Robot, Markdown, reST, other), shift introduction headings to level 3, replace `%TOC%` in Robot-format and Markdown documentation (sections with their subsections, a subsection before the first section left out, plus `Importing`/`Keywords`/`Data types`), resolve `[Name]` references and — with a new `resolve_backtick_links` in `markdown_docs.py` that skips fenced code — Robot-format and plain-text backtick names against the document's targets (keywords, types, Libdoc's fixed names, introduction headings of every format), then `document_links`, then the introduction's reference definitions (D4); in a `keywords` selection render references to omitted entries as inline code; assign anchors in one pass over the final headings with `Slugger` and build the outline in the same walk (D5); verify with unit tests for `resolve_backtick_links` in `test_markdown_docs.py` and renderer tests on RF 6.1 and 7.5: `BuiltIn`/`Collections` level-1/level-2 headings, introduction sections at level 3, no literal `%TOC%` and the expected TOC entries (on RF < 7.5 starting with `HTML error messages` for `BuiltIn`, no `Table of contents` entry), `[Set Log Level]` linked (RF ≥ 7.5), `[BuiltIn]` in `Collections` linked to the URL of its reference definition without `document_links` and to the file with them (RF ≥ 7.5), `` `Should Be Equal` `` linked (RF < 7.5), every link target equal to exactly one anchor, no `\#` in link targets, table rows with their header's cell count, `> **Warning**` in `Collections` (RF ≥ 7.5), a heading that repeats a keyword name, `find_entry("keyword", "do_thing")`, the outline order of the fixture library, and a `-k`-style selection without dangling links
- [ ] 4.4 Verify reproducibility: a test that renders `Collections` in two subprocesses (`sys.executable`) with `PYTHONHASHSEED` 1 and 2 and compares the texts; then run `hatch run test:test -- tests/robotcode/robot` on the full RF matrix and confirm the existing rendering tests are unchanged, and confirm that `KeywordDoc.to_markdown()` of BuiltIn `Log` on RF 6.1 and RF 7.5 is byte-identical to the output saved before 2.1 (requirement "Existing documentation surfaces keep their rendering")
- [ ] 4.5 For each of `LibraryDoc.loaded_without_arguments` (`library-loading-robustness`) and `LibraryDoc.runtime_only_args` (`library-keyword-set-declaration`) that exists when this task is implemented: render its note after the load errors at the start of `## Importing` (D6) and add a scenario for it to the requirement "Load errors are part of the document" in `specs/library-documentation-markdown/spec.md`; a marker whose change is applied later is added by that change's task 4.3; verify with renderer tests in `test_documentation_markdown.py` on a `LibraryDoc` with the marker set (note present, `Importing` present) and without it (no note), and `openspec validate doc-cli` — if neither field exists yet, mark this task as not applicable

## 5. Command

Tasks 5.1–5.6 assume Q3 (a), the command in `robotcode-repl`; with (b) or (c) the same work goes into that package, and 5.1 names the dependency change of (c). The command name follows Q2.

- [ ] 5.1 If Q3 is (c): move `prompt_toolkit` from `packages/repl/pyproject.toml` to `packages/runner/pyproject.toml`; with (a) (recommended) or (b) no package gets a new dependency. No variant adds `robotcode-analyze` to a package, because the configuration loading, `get_analyze_config` and `merge_variable_and_path_options` come from `robotcode-robot` (D7); verify that `git diff -- 'packages/*/pyproject.toml'` shows no change with (a) or (b) and only that move with (c)
- [ ] 5.2 Create the command (`packages/repl/src/robotcode/repl/doc_cli.py`, registered in `repl/hooks.py`) with the targets and the options of D8, split targets on `::`, tell resources from libraries by `ALLOWED_RESOURCE_FILE_EXTENSIONS`, resolve them through the root folder's `ImportsManager` of a `DocumentsCacheHelper` built as in D7 (configuration loaded with `load_robot_config_from_path` without `extra_tools`, or with `extra_tools=BUILTIN_TOOL_CONFIG_CLASSES` if `analyze-config-in-robot` decided its Q4 as (b); `[tool.robotcode-analyze]` read with `get_analyze_config` from `robotcode.robot.config.analyze_config`; `-v/-V/-P` merged into the combined profile with `merge_variable_and_path_options` from `robotcode.robot.config.utils`; the python path; a `Workspace` with one folder for the project root and a Robot file reader; `FileWatcherManagerDummy`; nothing from `robotcode.analyze`) with the current directory as base dir, and write the documents through `app.echo_as_markdown`; verify with a new `tests/robotcode/repl/test_doc_cli.py` (in-process `CliRunner` with `--no-color --no-pager`, a `tmp_path` project with `robot.toml`, `ROBOTCODE_CACHE_DIR` in `tmp_path`, `monkeypatch.chdir`, an `ArgLib` whose `mode` argument defaults to `a`): `MyLib` through `python-path`, `ArgLib::${MODE}` → `Kw B`, `-v MODE:c` with an empty cache → `Kw C`, `[tool.robotcode-analyze.cache] ignore-arguments-for-library = ["ArgLib"]` in a `robot.toml` that also has a `[tool.robotcode-analyze.code]` table → `ArgLib::${MODE}` with an empty cache documents `Kw A`, a variable from `-V vars.py` and one from a profile selected with the global `-p`, a library found only through `-P`, a library and a resource in one call in order, a `.robot` suite file, a relative path from a subdirectory, a usage error with exit code 2 without targets, and a `sys.executable` subprocess that imports the command module and finds no `robotcode.analyze` module in `sys.modules`
- [ ] 5.3 Implement `-o/--output` (D9): a file with all documents, or a directory with `<name>.md`, `-2`/`-3` suffixes, `document_links` for cross-links, UTF-8 and `\n` line endings, created when missing; verify in `test_doc_cli.py`: one file for two targets, `BuiltIn Collections` into a directory on RF ≥ 7.5 with `[BuiltIn]` in `Collections.md` linking to `BuiltIn.md` (not to the URL of its reference definition), `a/common.resource b/common.resource` → `common.md`/`common-2.md`, and `read_bytes()` containing no `\r\n`
- [ ] 5.4 Implement `-k/--keyword` with `MultiMatcher`, `--list` with `short_doc`, `--search`/`--search-regex` with `make_search_matcher` from `robotcode.runner.cli._search` and `make_md_highlighter` from `robotcode.runner.cli._markdown` for keyword names, the error on stderr with exit code 1 for load errors and for a `-k` without any match, and "nothing found" with exit code 0 for a search; turn an exception of the resolution into a document with the target's name and the error (D7); verify in `test_doc_cli.py` with every scenario of the requirements "Keyword selection", "Keyword listing", "Search across documents" and "Load errors and exit code", and with a resource target `missing.resource` that does not exist (document with the error in `Importing`, error on stderr, exit code 1, the other targets documented)
- [ ] 5.5 Implement the mode selection of D8 (browser only for TTY stdin and stdout and none of `-o`, `-k`, `--list`, search; without `--tui/--no-tui` only outside an AI-agent session, with `--tui` also inside one; `--tui` without a terminal is a usage error); verify in `test_doc_cli.py` by patching the TTY and agent checks in the command module (not where they are defined) and asserting that the browser is started with the rendered documents or that the Markdown is echoed, for the default, `--no-tui`, `--tui` in an agent session and `--tui` in a pipe (exit code 2)
- [ ] 5.6 If Q6 is confirmed: emit `{"documents": [...]}` and `{"keywords": [...]}` through `app.print_data` for `--format json|json-indent|toml`, add the requirement to `specs/documentation-cli/spec.md` and verify with `test_doc_cli.py` cases for both shapes; if not, nothing to implement (the global `--format` is ignored as by `robotcode libdoc`)

## 6. Terminal browser

- [ ] 6.1 Split `DocViewer` in `packages/repl/src/robotcode/repl/_pt/doc_viewer.py` into an embeddable pane (key bindings active while the pane has focus, `jump_to(anchor)` pushing the back stack) and the existing full-screen wrapper with an unchanged `run(title, markdown, scroll_to=…)`; verify that `tests/robotcode/repl/test_doc_viewer.py`, `test_prompt_toolkit_interpreter.py` and `test_dot_commands.py` pass unchanged and a new test covers `jump_to` + back
- [ ] 6.2 Create `packages/repl/src/robotcode/repl/_pt/doc_browser.py` (D10): sidebar per document with introduction sections, `Importing`, `Keywords (shown/total)` with filter, `Data types`, `Tags`; Enter/click jumps the pane (loading another document first when needed), tag entries filter keywords, a key toggles the sidebar and it starts hidden below 80 columns, focus moves between sidebar and pane, the footer shows the keys; verify with a new `tests/robotcode/repl/test_doc_browser.py` under the autouse `create_pipe_input` + `DummyOutput` session of `tests/robotcode/repl/conftest.py`, calling the application's state and key-binding handlers as `test_doc_viewer.py` does (entries and counts of a fixture document, filter `dict` on `Collections`, a tag filter, jump + back, switching between two documents, the sidebar toggle and the narrow-terminal default)
- [ ] 6.3 Start the browser from the command's interactive mode; verify manually with `robotcode doc Collections String` in a terminal on Linux, in Windows Terminal and on macOS (jump, filter, search, back/forward, links, resize)
- [ ] 6.4 If Q4 is confirmed: let `.doc` in `packages/repl/src/robotcode/repl/console_interpreter.py` render the canonical documentation (project root = the REPL's root) and open the browser on the prompt_toolkit backend, the plain backend paging the canonical Markdown through `show_doc`; add the requirement to `specs/documentation-cli/spec.md` and update the `.doc` rows of `docs/03_reference/repl.md`; verify with tests in `test_dot_commands.py` (plain backend output starts with `# Collections`) and `test_prompt_toolkit_interpreter.py` (browser started with the session's library), and manually at a `robot-debug` stop

## 7. Documentation and agent skill

- [ ] 7.1 Write `docs/03_reference/browsing-documentation.md` ("Browsing Library Documentation", in the style of `discovering-tests.md`: installation tip for the hosting package, targets and `::` arguments incl. the variable recipe for values with `::`, configuration and the shared disk cache, the browser and its keys, Markdown/file/directory output, `-k`/`--list`/`--search`, the canonical structure and its anchor rule, why several documents belong in directory output rather than one file, exit codes), add it to `docs/03_reference/index.md` and the package overview of `docs/03_reference/cli.md`, and regenerate the command reference with `hatch run create-cmd-line-docs` in the default environment on the newest supported Robot Framework, keeping only the hunks of the new command (CONTRIBUTING.md); leave `docs/03_reference/ai-agents.md` as it is unless Q8 decides otherwise; verify with `npm run docs:build`
- [ ] 7.2 Update the `robotcode` skill in /home/daniel/develop/robot/robotframework-agent-plugins (`plugins/robotcode/skills/robotcode/SKILL.md` "Documentation lookup priority", "Library & keyword information", the documentation gotcha under "Gotchas — agent-correction notes" and the other sentences that name `libdoc` as the lookup, `references/authoring.md`, `repl.md`, `workflows.md`, `install.md`) to `robotcode doc <Lib> --list`, `robotcode doc -k "<Keyword>" <Lib>` and `robotcode doc --search <text> <Lib>…` with the providing package and `robotcode libdoc … list/show` as fallback; then run `hatch run build:sync-chat-plugin` in this repo; verify with `hatch run build:sync-chat-plugin --check` and by running every recipe of the skill against a project on RF 7.5

## 8. Verification

- [ ] 8.1 Run `hatch run lint:all` and `hatch run test:test` (RF 5.0–7.5) and confirm both pass with no regression-baseline changes; confirm the CI matrix on Linux, Windows and macOS is green
- [ ] 8.2 Manually generate `robotcode doc -o site/ BuiltIn Collections String` on RF 6.1 and RF 7.5 and check the files in GitHub's Markdown preview (headings, TOC and cross-file links work, tables render), run `robotcode doc --search dictionary Collections BuiltIn` with `CLAUDECODE=1` (raw Markdown, no pager, no browser), and check in VS Code on RF 7.5 that the library hover shows `GLOBAL` and the keyword hovers are unchanged
